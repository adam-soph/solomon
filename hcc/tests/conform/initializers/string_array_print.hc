// String array init; print the array as a string and check the length.

#include <stdio.hh>
#include <string.hh>
U8 s[] = "hello";
"%d\n", StrLen(s);
I64 i;
for (i = 0; i < 5; i++) "%d ", (I64)s[i];
"\n";
