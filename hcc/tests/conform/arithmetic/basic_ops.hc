// Integer arithmetic across the basic binary operators.

#include <stdio.hh>
I64 a = 17, b = 5;
"%d %d %d %d %d\n", a + b, a - b, a * b, a / b, a % b;
"%d %d %d\n", a & b, a | b, a ^ b;
"%d %d\n", a << 2, a >> 1;
