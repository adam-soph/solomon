// switch with negative case values.
I64 x = -2;
switch (x) {
  case -3: "neg three\n"; break;
  case -2: "neg two\n"; break;
  case -1: "neg one\n"; break;
  case 0: "zero\n"; break;
  default: "other\n";
}
